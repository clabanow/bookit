/**
 * Soccer Stud Game Layout
 *
 * Nesting point for all soccer routes (/games/soccer/*).
 * Currently a pass-through — soccer-specific shared UI goes here later.
 */

export default function SoccerLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
