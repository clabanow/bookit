/**
 * Games Layout
 *
 * Nesting point for all game routes (/games/*).
 * Currently a pass-through — future shared game UI (nav bar, etc.) goes here.
 */

export default function GamesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
